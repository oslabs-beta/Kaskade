import React from "react";
import { render } from "react-dom";
import styled from "styled-components";

const ParamsTab = (props) => {
    return (
        <div><p>This is the Params tab</p></div>
    )
}

export default ParamsTab;